# LaTeX 项目管理器 v0.7.0

本次版本重点重构 Android 阅读与下载体验。

## Android PDF

- 修复部分 PDF 下载完成后无法打开、重新打开失败的问题。
- 限制 PDF 单页渲染内存，避免超大或异常页面导致应用闪退。
- 单页渲染失败不会再带崩整个查看器，并可切换到其他 PDF 应用打开。
- 增加“重新下载”恢复入口，损坏缓存会先安全删除再重新获取。
- 重新打开同一路径的 PDF 时会强制重建系统渲染器，不复用旧失败状态。

## 下载

- 下载临时文件可跨网络重试保留，支持 HTTP Range 断点续传。
- GitHub Media、Raw 与 Contents API 多下载源自动切换。
- 自动识别 Git LFS 指针、HTML 错误页、空文件和无效 PDF。
- 响应过慢时保留已下载字节并切换备用源。
- 相同文件的重复点击会合并为同一个后台任务，避免互相争抢带宽。
- 下载进度显示实时速度，可隐藏；完整状态仍可在“下载”页和系统通知中查看。
- 下载完成提示改为非阻塞横幅，可直接打开或关闭。

## 界面

- 项目库改为简洁的扁平文件夹列表，接近现代文件管理器布局。
- 项目下载和移除收进“更多”菜单，减少视觉噪音和误触。
- 文件列表与下载列表统一为轻量行式布局，增加留白并减少厚重卡片。
- 浅色主题改用白色主背景和更克制的分隔线。

Android 下载仍会受到当前网络到 GitHub 的实际带宽影响，但切换下载源和断点续传能够避免慢源长期卡住或失败后从零开始。
