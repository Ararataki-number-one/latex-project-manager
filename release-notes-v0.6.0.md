# LaTeX 项目管理器 v0.6.0

v0.6 重新整理了桌面端和 Android 端的信息层级，继续坚持“项目管理器，而不是 LaTeX 编辑器”的产品定位。

## 桌面端

- 项目库只常驻“管理”和“更多”，复制、导出、清理、归档与移除集中到操作菜单。
- 新增全局同步中心，集中查看已连接、处理中和需要处理的项目。
- 项目页简化为“项目介绍、原始文稿、同步”，文档入口与方案改为当前页面上下文。
- GitHub 同步页默认只展示状态与待同步文件，高级仓库设置和同步时间线按需展开。
- 设置拆分为“账号与同步、客户端更新、关于”。
- 修复窄屏项目标题栏被压缩、操作按钮裁切等响应式问题。

## Android 端

- 新增“首页、项目、下载、设置”底部导航。
- 首页优先显示继续阅读，并展示最多三个最新主 PDF。
- 已缓存 PDF 立即打开，同时在后台检查 GitHub 最新 SHA；发现更新后自动换用新版。
- 项目卡片改为名称在上、信息与操作在下的简洁布局。
- 新增下载中心，明确显示默认下载位置和本次下载文件。
- PDF 查看器改为紧凑工具栏，并显示当前页码；从下载中心打开后可正确返回下载中心。
- 设置页集中管理 GitHub 访问令牌、自动更新和 PDF 离线缓存。

## 安装说明

- Windows 安装版不需要管理员权限；便携版可以直接运行。
- Android APK 使用与 v0.5 相同的固定签名密钥，可直接覆盖升级正式版。
- Android 仍为只读客户端，不会修改、提交或删除 GitHub 文件。
