# LaTeX 项目管理器 v0.5.0

v0.5 将桌面端的 GitHub 自动同步和 Android 主 PDF 阅读连成了可信闭环。

## Windows 桌面端

- 关闭窗口后默认缩到托盘继续同步；托盘可同步全部、暂停/恢复和退出。
- 文件停止变化约 10 秒后自动提交新增、修改和删除；每项目单任务，全局最多两个并行任务。
- 推送前获取远端状态，只在工作区干净且可快进时执行 `pull --ff-only`，绝不自动合并、变基或强推。
- 断网按 30 秒、2 分钟、5 分钟重试；增加排队中、重试中和安全阻止状态。
- 上传前扫描私钥、GitHub/AWS/API 令牌、凭据文件及未使用 Git LFS 的大文件。
- 每项目保留最近 100 条同步记录；阻止和最终失败会发送 Windows 通知。
- 项目介绍页可设置每个文档目标及项目默认的移动端主 PDF，安全写入 `.latex-project.json`。
- 新增首次使用向导和托盘/全局同步设置；删除不再使用的文档结构与配置界面代码。

## Android 只读端

- 首页顺序改为“继续阅读、最新 PDF、全部项目”。
- 读取 `.latex-project.json`，打开前始终检查 GitHub 文件 SHA；未变化复用缓存，变化时下载新版。
- 保存每仓库、每 PDF 的阅读页码；新版页数减少时自动限制到有效范围。
- PDF 缓存改为 512 MB LRU，并在设置中显示占用与提供清理按钮。
- 继续支持多项目、内置 PDF/代码查看器、单文件下载、整项目 ZIP 和应用内更新。

## 安全与兼容

- 不改写 `.tex`、`.bib`、`.cls`、`.sty` 等 LaTeX 源文件。
- 旧项目无需 `.latex-project.json` 也可继续管理和浏览。
- Android 仍严格只读，不编辑、提交或删除 GitHub 文件。
