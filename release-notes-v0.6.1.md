# LaTeX 项目管理器 v0.6.1

这是 Android 下载与 PDF 阅读的可靠性修复版本，同时包含 v0.6.0 之后的桌面端导入、新手引导和同步中心 UI 优化。

## Android

- 文件、项目 ZIP、主 PDF 和应用更新迁移到 WorkManager 持久后台任务。
- 下载过程中熄屏、切换应用或短时断网不再丢失任务；通知栏持续显示进度并可取消。
- 重新打开应用后恢复正在进行的任务和最近下载记录。
- GitHub 二进制文件优先使用 media 下载入口，并将流式缓冲增大到 1 MB，减少大 PDF/ZIP 的额外开销。
- 每次打开 PDF 前仍检查 GitHub 最新 SHA，只有内容变化才下载新版。
- PDF 下载完成后先校验并原子写入缓存；从下载目录阅读时会先复制为可随机访问的本地文件。
- PDF 解析移至后台线程，并针对加密、损坏和不可随机访问文件显示明确原因。
- APK 更新也使用后台下载，完成后继续校验文件大小与 SHA-256，再打开系统安装界面。

## 桌面端

- 导入项目改为“选择目录、选择项目、导入方式”三步流程。
- 新手向导补充明确步骤、可跳过 GitHub 登录和本地管理说明。
- 同步中心正确区分待同步、同步中、需要处理和已连接。
- 修复 Windows 高 DPI 环境下的初始窗口尺寸和品牌标志溢出。

## 安装

- Android APK 继续使用固定签名密钥，可直接覆盖升级 v0.5/v0.6 正式版。
- 首次启用后台下载通知时，Android 会询问通知权限。
