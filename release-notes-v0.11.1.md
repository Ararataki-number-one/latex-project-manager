# LaTeX 项目管理器 0.11.1

这是 1.0 开发前的安全修复版本。

- 撤销快照目录永久排除在 Git 自动同步之外。
- 如果撤销快照已经被 Git 跟踪，同步会立即安全阻止并提示处理。
- 文件重命名、移动、复制和回收站操作的撤销记录可在客户端重启后继续使用。
- 24 小时后自动清理过期撤销快照。
- 仅浏览“原始文稿”时不再创建 `references` 文件夹，也不会产生无意的 Git 变化。

此版本不迁移项目数据，也不改变现有界面。
